# offckb-py-intergration-test 工程说明

本文件是该独立测试工程的唯一规范性指令来源。

## 目标

- 从普通用户借助 OffCKB 快速上手 CKB 的流程定义场景：启动开发链、使用账户和资产、创建并运行合约项目、调试问题、保存或重置环境。优先验证用户能完成的事情；退出码、PID、文件和链上状态是验证依据，低频内部状态异常不抢占入门主流程的优先级。不扩展为 CKB 共识、脚本规则或协议边界的测试矩阵。
- 让测试意图便于人工阅读和评审。
- 让可执行测试独立于产品源码仓库。
- 评审资料以中文为主；命令、路径、JSON 字段、代码标识和 `TEST-MAP` 保持原始英文。
- 从评审用例到测试代码只使用一个稳定 ID，例如 `RPC-01`。
- 根据代码中的映射计算自动化覆盖情况，不维护状态台账或额外映射表。

## 被测目标

- 源码仓库：`https://github.com/ckb-devrel/offckb.git`
- 本地源码选择：`--offckb-source` / `OFFCKB_SOURCE` 显式指定优先，其次 `source/offckb/`，最后同级 `../offckb/`。
- 测试工程独立于产品仓库，禁止将本机源码链接、Python 虚拟环境、CKB 二进制或运行产物提交到测试仓库。
- 默认版本：`config/offckb.toml` 中的 `latest`，对应 npm 上最新发布的 `@offckb/cli`；设为 `develop` 可测试官方开发分支，`OFFCKB_REPO` / `OFFCKB_REF` 可覆盖源码仓库和版本选择。
- `make prepare` 下载并校验最新发布包，或将所选 Git 版本解析为 commit 并构建发布包；`make test` 校验并使用已准备的包。`latest` 不要求源码；源码模式下源码目录只用于复用 Git 对象，不隐式决定被测分支；只有显式 `working-tree` 才包含本地未提交修改。
- 被测对象：打包后的 `@offckb/cli`、其 `offckb` 可执行程序、由它管理的 CKB node/miner/RPC proxy、生成的项目文件、持久化 OffCKB 状态以及产生的链上交易和 cell。
- 稳定入口：`offckb --json ...`、direct CKB JSON-RPC、OffCKB RPC proxy，以及独立 OffCKB config/data/cache 根目录下的文件。
- 参考资料：`source/offckb/README.md`、`source/offckb/docs/develop.md`、`source/offckb/package.json`、`source/offckb/src/cli.ts`，以及 `source/offckb/templates/`、`source/offckb/ckb/devnet/` 中随包发布的模板和配置。

这里只保存长期稳定的共同事实。具体行为用例放在评审文档中。

### 黑盒边界

- 下载已发布 npm 包或从所选源码构建 npm 包，并调用安装后的 `offckb` 可执行程序。集成测试不得导入 `src/` 内部模块。
- 只把 CLI 退出码、stdout、stderr、RPC 响应、文件、进程生命周期和已确认的链上状态作为观察结果。
- JSON 模式下，stdout 必须只有一个命令结果 JSON；进度通过 stderr 输出 NDJSON；失败必须结构化并返回非零退出码。
- 返回交易哈希不代表成功。必须轮询 `get_transaction`，直到交易 committed 或 rejected。

### 隔离和进程归属

- 每次运行使用全新的临时用户目录和临时 npm prefix，禁止全局安装被测包。
- Unix 类系统设置 `HOME` 和全部 XDG 根目录；Windows 设置 `APPDATA` 和 `LOCALAPPDATA`。禁止读取、清理、fork 或停止开发者真实的 OffCKB 数据。
- 核心测试使用预先选定的真实本地 CKB 二进制，不依赖网络下载。默认启动及版本选择场景让 OffCKB 自行选择隔离目录中已准备的托管二进制；其余节点场景可通过 `--binary-path` 指定本地二进制。
- Devnet 当前使用固定端口，主要包括 `8114`、`28114`、`18114` 和 `8115`；同一 runner 内拥有 devnet 的测试必须串行执行。
- 共享 devnet fixture 优先通过 `offckb node --daemon` 启动、通过 `offckb node stop` 停止；前台使用场景单独验证普通 `offckb node` 和用户中断。teardown 必须确认所属进程已退出、相关 PID 元数据已删除且端口已释放。
- readiness 和完成状态必须使用带截止时间的 RPC/状态轮询。固定时长的 sleep 不能作为服务或交易已经就绪的证据。

### 私钥和外部依赖

- 私钥只通过 `--privkey-file` 或 `OFFCKB_PRIVATE_KEY` 传入，不得出现在命令参数或测试日志中。
- 内置私钥只用于 devnet fixture。
- 网络下载、公共网络/faucet、完整数据库 fork、TTY 流程和长时间兼容/稳定性矩阵必须使用明确 marker，不属于普通 PR 门禁。

### 产品内部测试

- 源码仓库的 typecheck、lint、Jest、build 和现有 shell 流程属于更早一层质量门禁，不是本工程的黑盒用例。
- 可以复用内部测试中稳定的 fixture 和外部可观察 oracle，但不得把 mock 实现断言复制到集成测试。
- 在等价 pytest 用例完成迁移前，现有 shell 集成流程可以继续作为兼容门禁。不得仅为了声称具有黑盒覆盖而从 pytest 调用 shell 测试。

## 目录结构

当前只有一个集成测试方式，使用根目录下的 `reviews/`、`tests/`、`config/` 和 `fixtures/`。

已初始化方式：`integration`。

- 根评审和执行范围：服务生命周期、跨组件流程、外部接口和端到端结果。

## 人工评审约定

每个完整接口或连贯行为使用一个评审文档。主表必须严格保持以下格式：

```markdown
| 用例 | 场景 | 预期结果 | 防止的问题 | 优先级 |
| --- | --- | --- | --- | --- |
| `RPC-01` | [场景] | [可观察结果] | [防止的具体问题] | P0 |
```

- 每行必须自包含，并使用清晰的产品语言。
- 用例 ID 在整个工程中唯一；评审用例 ID 就是唯一的 Test Point ID。
- 修正描述、预期或优先级时保留原 ID。
- 过时用例直接修改或删除，不增加批准、覆盖率或自动化状态。
- 主表中不写测试路径、实现计划、内部 ID 链或执行历史。
- 行为有歧义时，在预期结果中写 `待确认：<决定>`，并在文档的“本轮需要确认”中列出。

## 强制评审门

每个新增、删除或实质变化的评审用例行都必须执行：

1. 更新完整的评审用例行集合。
2. 展示变更内容，并在修改自动化测试前暂停。
3. 等待人工明确确认。
4. 如果收到修正意见，应用修正；预期发生实质变化时再次展示。
5. 只有确认后才能生成或修改对应的映射测试代码。

不得在一次交付中合并评审和自动化实现。已建立工程中未变化的既有用例仍可实现。确认只保留在对话中，不在文档里增加状态。

## 评审反馈

创建或修订用例前，必须读取对应的 `reviews/review-feedback.md`（如果存在）。

当人工修正 AI 生成的用例，例如要求补充或删除用例、修改场景或预期、调整优先级、合并、拆分、重命名或删除时，向对应评审目录的 `review-feedback.md` 追加一行：

```text
- model: <model-id-or-unavailable> | cases: <case IDs or review scope> | feedback: <human feedback verbatim>
```

第一次收到修正时创建文件。保留反馈原文，只把换行折叠为空格，并将字面量 `|` 转义为 `\|`。没有修正意见的单纯批准不记录。该文件用于积累可复用的分析反馈，不是用例状态、批准历史、解决日志或执行历史。

## 自动化映射

每个已映射的自动化测试旁边必须有一条使用对应语言注释语法的注释，其内容严格为：

```text
TEST-MAP: <CASE-ID>
```

场景和 oracle 已经存在于评审用例和断言中，不要在映射注释里重复。运行：

```bash
python3 scripts/check_test_map.py
```

检查器会计算已映射和未映射用例、重复评审 ID、孤立代码映射以及一对多映射。不得把计算结果写回评审文档。

## 源码工作区

- 优先使用显式指定的源码，其次复用 `source/offckb/` 或同级 `../offckb/` 下匹配的源码目录。
- 只有声明的源码目录不存在时才克隆。
- 不得覆盖冲突路径。
- 产品源码属于长期本地数据，由 `.gitignore` 排除。
- 分析 PR 时先检查 base/head diff，再按需读取更大源码上下文。

## 日常维护

- 修改测试前先阅读对应评审文档。
- 分析覆盖前先阅读相关历史修正反馈。
- 使用 `TEST-MAP: <CASE-ID>` 搜索映射代码，不创建独立映射台账。
- 产品行为变化时先更新评审用例行，然后停在强制评审门。
- 确认后再更新自动化，运行聚焦测试和映射检查器。
- 在对应 README 中保持 setup、完整运行和聚焦运行命令为最新状态。
- 不创建每 PR 报告目录、状态历史、批准台账或结果归档。

## 交付格式

只报告相关字段：

```text
评审范围：<测试域、接口、行为或 PR>
变更用例：
- <ID> [P0/P1/P2] <场景> -> <预期结果>
自动化覆盖：<已映射>/<已评审>；未映射：<IDs 或无>
验证：<命令> -> <结果和退出码>
残余风险：<有歧义、需手工、不可观察或无>
需要评审：<具体产品决定或无>
下一门：<需要明确评审确认，或下一步实现/验证动作>
```

无关字段直接省略，不输出占位内容。
